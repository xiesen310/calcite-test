{
  "version": "1.0",
  "defaultSchema": "TEST_CSV",
  "schemas": [
    {
      "name": "TEST_CSV",
      "type": "custom",
      "factory": "org.example.calcite.test.customer.CsvSchemaFactory",
      "operand": {
        "dataFile": "TEST01.csv",
        "ddl": "CREATE TABLE USERS ( ID VARCHAR, NAME1 VARCHAR, NAME2 VARCHAR )"
      }
    }
  ]
}
