{
  "version": "1.0",
  "defaultSchema": "TEST",
  "schemas": [
    {
      "name": "TEST",
      "type": "custom",
      "factory": "org.example.calcite.test.customer.ZorkSchemaFactory",
      "operand": {
        "tables": [
          {
            "dataFile": "TEST01.csv",
            "ddl": "CREATE TABLE USERS ( ID VARCHAR, NAME VARCHAR, DEPT_ID VARCHAR )"
          },
          {
            "dataFile": "TEST02.csv",
            "ddl": "CREATE TABLE DEPT ( ID VARCHAR, NAME VARCHAR)"
          }
        ]
      }
    },
    {
      "name": "XS",
      "type": "custom",
      "factory": "org.example.calcite.test.customer.ZorkSchemaFactory",
      "operand": {
        "tables": [
          {
            "dataFile": "TEST01.csv",
            "ddl": "CREATE TABLE USERS ( ID VARCHAR, NAME VARCHAR, DEPT_ID VARCHAR )"
          },
          {
            "dataFile": "TEST02.csv",
            "ddl": "CREATE TABLE DEPT ( ID VARCHAR, NAME VARCHAR)"
          }
        ]
      }
    }
  ]
}
